{
  "name": "fake-dialogs-editor",
  "version": "1.1.1",
  "description": "Fake JSON dialog files editor",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds",
    "package-win": "electron-packager . --overwrite --asar --platform=win32 --arch=ia32 --prune=true --out=release-builds  --version-string.CompanyName=Morbo --version-string.FileDescription=\"Utility for create/edit fake dialogs JSON schemas\" --version-string.ProductName=\"Fake Dialogs Editor\"",    
    "package-linux": "electron-packager . --overwrite --asar --platform=linux --arch=x64 --prune=true --out=release-builds"
  },
  "author": "ArturkA",
  "license": "MIT",
  "dependencies": {
    "semantic-ui-css": "^2.4.0"
  },
  "devDependencies": {
    "electron": "^3.0.4",
    "electron-packager": "^12.2.0"
  }
}
